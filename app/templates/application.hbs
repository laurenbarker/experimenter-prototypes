<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed"
      data-toggle="collapse" data-target="#menu-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      {{ link-to 'Home' 'index' class="navbar-brand" }}
    </div>

    <div class="collapse navbar-collapse" id="menu-collapse">
      <ul class="nav navbar-nav">
        <li>{{ link-to 'Consent Form' 'consent' }}</li>
        <li>{{ link-to 'Survey Question' 'question' }}</li>
        <li>{{ link-to 'Dynamic Survey Question' 'dynamic' }}</li>
      </ul>
    </div>
  </div>
</nav>

{{#if isIndex}}
	{{outlet}}
{{else}}
	<div class="container">
	  {{outlet}}
	</div>
{{/if}}
