<div id='input' class="test"> 

	<button class="btn btn-primary" {{action 'addConsentForm'}}>insert consent form</button>
	<button class="btn btn-primary" {{action 'addQuestion'}}>insert question</button>
	<button class="btn btn-primary" {{action 'addDynamicQuestion'}}>insert dynamic question</button>

	<hr>

	{{#each rangeOfConsentForms as |index|}}
		{{#if index}}
			{{ consent-form index=index}}
		{{/if}}
	{{/each}}
		
	{{#each rangeOfQuestions as |index|}}
		{{#if index}}
			{{ survey-question index=index}}
		{{/if}}
	{{/each}}

	{{#each rangeOfDynamicQuestions as |index|}}
		{{#if index}}
			{{ dynamic-questions index=index}}
		{{/if}}
	{{/each}}

</div>
